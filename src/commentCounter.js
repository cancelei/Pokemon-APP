export function countComments(commentCounter) {
    const commentList = document.querySelector('.counter');
    if(commentCounter == ''){
    commentList.textContent = commentCounter; 
    }else{
    commentList.textContent =commentCounter; 
    }
  }
  
